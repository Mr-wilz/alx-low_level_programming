#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiply two integers
 * @a: first parameter
 * @b: second parameter
 * Return: 0 (Success)
 */

int mul(int a, int b)
{
	return (a * b);
}
